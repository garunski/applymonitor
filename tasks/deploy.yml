version: '3'

tasks:
  prod:
    desc: Test deploy both API Worker and web to the same Pages project
    cmds:
      - task: prod:build-api
      - task: prod:deploy-pages
      - task: prod:deploy-api

  prod:build-api:
    desc: Build API Worker
    dir: "{{.API_DIR}}/main"
    internal: true
    cmds:
      - worker-build

  prod:deploy-pages:
    desc: Deploy Pages
    internal: true
    cmds:
      - |
        echo "🚀 Testing deployment of API Worker + Web to same Pages project"
        echo ""
        echo "📦 Web assets already built in target/dx/web/release/web/public"
        echo ""
        echo "🔧 Step 1: Deploying static assets to Cloudflare Pages..."
        wrangler pages deploy target/dx/web/release/web/public \
          --project-name=applymonitor-web-production
        echo ""

  prod:deploy-api:
    desc: Deploy API Worker
    dir: "{{.API_DIR}}/main"
    internal: true
    cmds:
      - |
        echo ""
        echo "🔧 Step 2: Deploying API Worker..."
        echo "Note: After deploying, attach the Worker to the Pages project via:"
        echo "  - Cloudflare Dashboard: Pages > applymonitor-web-production > Settings > Functions"
        echo "  - Or use the Pages API to attach the Worker programmatically"
        echo ""
        wrangler deploy --env production
        echo ""
        echo "✅ Deployment complete!"

  prod:deploy-gmail-scanner:
    desc: Deploy Gmail Scanner Worker
    dir: "{{.API_DIR}}/gmail-scanner"
    internal: true
    cmds:
      - |
        echo ""
        echo "🔧 Deploying Gmail Scanner Worker..."
        wrangler deploy --env production
        echo ""
        echo "✅ Gmail Scanner Worker deployment complete!"

  db:migrate-personal:
    desc: Apply database migrations to personal D1 database
    dir: "{{.API_DIR}}/main"
    cmds:
      - wrangler d1 migrations apply DB --env personal --remote

  db:migrate-prod:
    desc: Apply database migrations to production D1 database
    dir: "{{.API_DIR}}/main"
    cmds:
      - wrangler d1 migrations apply DB --env production --remote

  db:reset-personal:
    desc: Reset the personal D1 database by dropping all tables and reapplying migrations
    dir: "{{.API_DIR}}/main"
    cmds:
      - |
        echo "🗑️  Dropping all tables from personal database..."
        wrangler d1 execute DB --env personal --remote --file ../scripts/drop_all_tables.sql || echo "Tables may not exist"
        echo "🆕 Reapplying all migrations..."
        wrangler d1 migrations apply DB --env personal --remote
        echo "✅ Personal database reset complete!"

  db:reset-prod:
    desc: Reset the production D1 database by dropping all tables and reapplying migrations
    dir: "{{.API_DIR}}/main"
    cmds:
      - |
        echo "⚠️  WARNING: This will reset the PRODUCTION database!"
        echo "🗑️  Dropping all tables from production database..."
        wrangler d1 execute DB --env production --remote --file ../scripts/drop_all_tables.sql || echo "Tables may not exist"
        echo "🆕 Reapplying all migrations..."
        wrangler d1 migrations apply DB --env production --remote
        echo "✅ Production database reset complete!"
